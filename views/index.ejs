<%- include("layout/header") %>
    <main>
        <form action="/" method="post">
            <input type="text" name="programmingLanguage" placeholder="Programming Language" required>
            <button type="submit">Add</button>
        </form>

        <ul>
            <% if (pLNames.length> 0) { %>
                <!-- <li>
                    <%= pLNames[pLNames.length - 1] %>
                </li> -->

                <% for (let x=0; x < pLNames.length; x++) { %>
                    <div class="lists">
                        <%= pLNames[x] %>
                    </div>
                    <% } %>
                        <% } else { %>
                            <p style="color: white;margin: 1rem;">No data found!</p>
                            <% } %>
        </ul>
    </main>
    <%- include("layout/footer") %>